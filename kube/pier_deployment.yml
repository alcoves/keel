apiVersion: v1
kind: Service
metadata:
  name: pier
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4000
  selector:
    app: pier
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pier
  labels:
    app: pier
    digitalocean.com/copy-identifier: bken
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pier
  template:
    metadata:
      labels:
        app: pier
    spec:
      containers:
        - name: pier
          imagePullPolicy: "Always"
          image: registry.digitalocean.com/bken/pier:464c3f3f8dc3c7e56c63e7fedab5037489c1b036
          envFrom:
            - secretRef:
                name: pier
          ports:
            - containerPort: 4000
